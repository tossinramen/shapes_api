{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        /* Module Resolution */
        "paths": {
            "@/*": ["./src/*"]
        },

        /* Runtime Environment */
        "lib": ["ESNext", "DOM", "DOM.Iterable"],
        "types": [
            "vite/client",
            "react",
            "react-dom",
            "node"
        ]
    }
}